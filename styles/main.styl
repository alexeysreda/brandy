@import 'reset.css';
$brown = #70250B;
* {
	margin: 0;
	padding: 0;
}

body {
	font-family: 'Montserrat', sans-serif;
	font-weight: 600;
	font-size: 16px;
	background: rgb(247, 237, 206);
}

.prev,
.next {
	position: fixed;
	top: 0;
	width: 15%;
	height: 100%;
	display: flex;
	align-items: center;
	justify-content: center;
	text-decoration: none;
	z-index: 999;
	color: rgba(255, 255, 255, .5);
	font-size: 8rem;
	font-family: monospace;
	transition: all .2s;
}

.prev:hover,
.next:hover {
	color: rgba(255, 255, 255, .8);
	text-decoration: none;
}

.prev {
	left: -25%;
}

.next {
	right: -25%;
}

.active .prev {
	left: 0;
}

.active .next {
	right: 0;
}

.container {
	margin: 10vh auto;
	max-width: 80%;
	display: grid;
	grid-gap: 10px;
	grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
	grid-auto-rows: 250px;
	grid-auto-flow: dense;
}

div {
	text-align: center;
	//padding: 1rem 0;
	color: white;
	font-size: 3rem;
	text-transform: uppercase;
	background: rgba(0, 0, 0, .2);
	overflow: hidden;
	padding: 0;
	display: flex;
	align-items: stretch;
	justify-content: center;
	border: 5px solid white;
	border-radius: 5px;
}

div img {
	width: 100%;
	height: 100%;
	display: block;
	object-fit: cover;
	object-position: center;
	transition: all .5s;
}

div.show::before {
	content: '';
	display: block;
	position: fixed;
	z-index: 555;
	width: 100vw;
	height: 100vh;
	top: 0;
	left: 0;
	background: rgba(0, 0, 0, .6);
}

div.show img {
	position: fixed;
	top: 0;
	left: 0;
	margin: 30px;
	align-self: center;
	object-fit: contain;
	z-index: 666;
	width: calc(100% - 60px);
	height: calc(100% - 60px);
	filter: drop-shadow(0 3px 15px black);
	cursor: url(../img/close.png), grab; /* custom cursor to signal close on click */
}

div:not(.show):hover img {
	cursor: pointer;
	transform: scale(1.3);
}

.horizontal {
	grid-column: span 2;
}

.vertical {
	grid-row: span 2;
}

.big {
	grid-column: span 2;
	grid-row: span 2;
}

.active div:not(.show) img {
	filter: blur(3px);
}

.header {
	width: 100%;
	height: auto;
	min-height: 100vh;
	//height: 300px;
	background: linear-gradient(to bottom, rgba(0,0,0,0.95) 0%, rgba(0,0,0,0.6) 20%, rgba(0,0,0,0) 100%), url('../img/header-bg.jpeg') no-repeat center center;
	background-size: cover;
}

.header__wrapper {
	//background: rgb(247, 237, 206);
	max-width: 80%;
	margin: 0 auto;
	padding-left: 15px;
	padding-right: 15px;
}

.header__logo {
	width: 150px;
}

.header__text {
	margin-left: 10px;
	display: flex;
	flex-direction: column;
	justify-content: flex-start;
	align-items: center;
	font-family: 'Arial Black', monospace;
	margin-right: 340px;
	& span:first-child {
		font-size: 36px;
		text-transform: uppercase;
		//position: relative;
		//&::after {
		//	position: relative;
		//	content: '';
		//	height: 6px;
		//	width: 140px;
		//	background: linear-gradient(
		//			to right,
		//			#70250B,
		//			#AC4421
		//	);
		//	margin: 0 auto;
		//	bottom: -5px;
		//	left: 10px;
		//
		//}
	}
	& span:last-child {
		font-size: 22px;
		margin-top: 3px;

	}
}

.line {
	//position: relative;
	//content: '';
	height: 6px;
	width: 140px;
	background: linear-gradient(
			to right,
			rgba(255, 255, 255, 1),
			rgba(255, 255, 255, 0.75)
	);
	margin: 0 auto;
	bottom: -5px;
	left: 10px;
}

.header__navigation {
	display: flex;
	flex-direction: row;
	justify-content: space-between;
	align-items: center;
	width: 100%;
}

.header__li {
	display: flex;
	flex-direction: row;
	justify-content: flex-start;
	align-items: center;
}

.header__link {
	font-size: 18px;
	display: flex;
	flex-direction: row;
	justify-content: flex-start;
	align-items: center;
	position: relative;
	&:hover, &:focus {
		//color: $brown;
		text-decoration: none;
		& .header__icon img {
			transform: scale(1.3);
		}
	}
}

.header__icon {
	width: 60px;
	height: 60px;
	border: 2px solid white;
	border-radius: 50%;
	display: flex;
	justify-content: center;
	align-items: center;
	margin-right: 5px;
	background-color: transparent;
	& img {
		object-fit: scale-down;
		width: 35px;
	}
}

.header__li:last-child .header__icon img {
	width: 45px;
}

.header__heading, .header__link, .header__text {
	background: linear-gradient(
			to right,
			rgba(255, 255, 255, 1),
			rgba(255, 255, 255, 0.85)
	);
	-webkit-background-clip: text;
	-webkit-text-fill-color: transparent;
	text-align: center;
}

h1 {
	//color: $brown;
	font-size: 72px;
	font-weight: 900;
	letter-spacing: 1.1;
	display: inline-block;
	line-height: 1.4em;
	text-align: center;
	margin: 160px auto 0 auto;
	width: 100%;
}


.link__order {
	width: 240px;
	height: 70px;
	border: 3px solid white;
	display: flex;
	justify-content: center;
	align-items: center;
	border-radius: 30px;
	margin: 50px auto 0 auto;
	color: white;
	font-size: 20px;
	text-transform: uppercase;
	font-weight: 700;
}